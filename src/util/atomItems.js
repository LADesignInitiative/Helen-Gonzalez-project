import { atom } from "nanostores";

export const CarouselPosition = atom(0);

export const TestimonialSideDirection = atom("");

export const TestimonialLightBoxContent = atom({});

export const TestimonialLightBoxDisplay = atom(false);
